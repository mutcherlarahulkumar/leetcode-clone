// What the client is allowed to see. Anything else stays in the logs.
export const ERRORS = {
  malformedJSON: "request body is not valid json",
  payloadTooLarge: "request body is too large",
  notFound: "not found",
  internal: "internal server error",
};
