# cp .env.example .env, then: node --env-file=.env index.js
# The Makefile reads DATABASE_URL from .env too.
DATABASE_URL=postgresql://user:password@host/db?sslmode=require
JWT_SECRET=generate-with-openssl-rand-hex-32
JWT_EXPIRES_IN=1d
PORT=3000
